import { DeviceArrayPipe } from './device-array.pipe';

describe('DeviceArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new DeviceArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
