import { GtTimeStampPipe } from './gt-time-stamp.pipe';

describe('GtTimeStampPipe', () => {
  it('create an instance', () => {
    const pipe = new GtTimeStampPipe();
    expect(pipe).toBeTruthy();
  });
});
