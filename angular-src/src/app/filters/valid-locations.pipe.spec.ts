import { ValidLocationsPipe } from './valid-locations.pipe';

describe('ValidLocationsPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidLocationsPipe();
    expect(pipe).toBeTruthy();
  });
});
